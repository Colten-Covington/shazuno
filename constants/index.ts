/**
 * Central export point for all application constants
 * 
 * Add your application-specific constants here
 */

// Example: export const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3000';
